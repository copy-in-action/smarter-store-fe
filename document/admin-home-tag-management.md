# 관리자 홈 태그 관리 기능 PRD

## 📋 개요

관리자가 공연 상세 페이지에서 홈 섹션 태그를 관리할 수 있는 기능을 추가합니다. 홈 화면에 노출될 공연들을 섹션별로 태그 지정하여 관리할 수 있습니다.

## 🎯 목표

- 공연 상세 페이지에서 현재 지정된 홈 태그를 확인
- 모달을 통해 섹션별 태그를 추가/수정/삭제
- 직관적인 UI로 태그 관리 프로세스 간소화

## 📐 기능 요구사항

### 1. 공연 상세 페이지 (PerformanceDetailView)

#### 1.1 현재 태그 표시
- **위치**: 공연 상세 정보 영역
- **데이터 소스**: `GET /api/admin/performances/{performanceId}/home-tags`
- **표시 형식**: `sectionDisplayName-tagDisplayName` 형태로 Badge 컴포넌트 표시
  - 이유: `tagDisplayName`만 사용하면 중복될 수 있음
- **예시**:
  ```
  홈 섹션 태그: [베스트 공연-베스트 뮤지컬] [신규 공연-이번주 신규] [추천 공연-로맨스]
  ```

#### 1.2 태그 관리 버튼
- **버튼 텍스트**: "태그 관리"
- **위치**: 공연 상세 페이지 액션 영역 (수정/삭제 버튼 근처)
- **동작**: 클릭 시 태그 관리 모달 열기

### 2. 태그 관리 모달

#### 2.1 모달 구조
- **제목**: "홈 섹션 태그 관리 - {공연명}"
- **크기**: 중간 크기 (max-width: 600px)
- **스크롤**: 섹션이 많을 경우 내부 스크롤

#### 2.2 섹션별 태그 선택
- **데이터 소스**: `GET /api/admin/home/sections/metadata`
- **구조**: 각 section 객체를 순회하며 표시
  ```typescript
  {
    section: "BEST_PERFORMANCE",
    displayName: "베스트 공연",
    tags: [
      { tag: "BEST_MUSICAL", displayName: "베스트 뮤지컬" },
      { tag: "BEST_CONCERT", displayName: "베스트 콘서트" }
    ]
  }
  ```

#### 2.3 각 섹션별 UI
- **섹션 제목**: `displayName` 표시 (예: "베스트 공연")
- **Multi-Select 컴포넌트**:
  - [shadcn-multi-select-component](https://github.com/sersavan/shadcn-multi-select-component) 사용
  - 각 섹션별로 다중선택 가능한 Select 컴포넌트 표시
  - `tags` 배열의 각 태그를 옵션으로 표시 (`displayName` 표시, `tag` 값 사용)
- **기본 선택값**:
  - 현재 공연에 지정된 태그들은 선택된 상태로 표시
  - 섹션당 여러 태그 동시 선택 가능

#### 2.4 저장 로직
- **변경사항 감지**: 초기값과 비교하여 변경된 태그만 처리
  - 추가된 태그: 기존에 없었는데 새로 선택된 태그
  - 삭제된 태그: 기존에 있었는데 선택 해제된 태그
- **추가**: `POST /api/admin/performances/{performanceId}/home-tags`
  ```typescript
  {
    tag: "BEST_MUSICAL" // 추가할 태그
  }
  ```
- **삭제**: 체크 해제된 태그는 `DELETE /api/admin/performances/{performanceId}/home-tags/{tag}`
- **일괄 처리**:
  - 모든 추가/삭제 API를 순차적 또는 병렬로 호출
  - **작업 중 UI 상태**:
    - 저장 버튼 비활성화 (로딩 스피너 표시)
    - 모달 닫기(X) 버튼 비활성화
    - 취소 버튼 비활성화
  - 모든 작업이 완료될 때까지 대기
- **성공 메시지**: "홈 태그가 성공적으로 저장되었습니다."
- **에러 처리**:
  - 일부 실패 시 성공한 작업과 실패한 작업 구분하여 표시
  - 전체 실패 시 "홈 태그 저장 중 오류가 발생했습니다." 메시지 표시

## 🎨 UI/UX 상세

### 공연 상세 페이지
```
┌─────────────────────────────────────┐
│ 공연 상세                            │
│                                     │
│ 제목: 레미제라블                     │
│ 카테고리: 뮤지컬                     │
│ 기간: 2024-01-01 ~ 2024-03-31       │
│                                     │
│ 홈 섹션 태그:                        │
│ [베스트 공연-베스트 뮤지컬]          │
│ [신규 공연-이번주 신규]              │
│                                     │
│ [수정] [삭제] [태그 관리] [목록으로]│
└─────────────────────────────────────┘
```

### 태그 관리 모달
```
┌──────────────────────────────────────────┐
│ 홈 섹션 태그 관리 - 레미제라블       [X]│
├──────────────────────────────────────────┤
│                                          │
│ 베스트 공연                               │
│ ┌────────────────────────────────────┐   │
│ │ ▼ 베스트 뮤지컬, 베스트 콘서트 ✓  │   │
│ │   • 베스트 뮤지컬 ✓                │   │
│ │   • 베스트 콘서트 ✓                │   │
│ │   • 베스트 연극                    │   │
│ └────────────────────────────────────┘   │
│                                          │
│ 신규 공연                                 │
│ ┌────────────────────────────────────┐   │
│ │ ▼ 이번주 신규 ✓                    │   │
│ │   • 이번주 신규 ✓                  │   │
│ │   • 이번달 신규                    │   │
│ └────────────────────────────────────┘   │
│                                          │
│ 추천 공연                                 │
│ ┌────────────────────────────────────┐   │
│ │ ▼ 선택해주세요                      │   │
│ │   • 로맨스                         │   │
│ │   • 액션                           │   │
│ └────────────────────────────────────┘   │
│                                          │
│              [취소] [저장 중...]         │
│              (작업 중 버튼 비활성화)      │
└──────────────────────────────────────────┘
```

## 🔌 API 엔드포인트

### 1. 공연의 홈 태그 조회
```
GET /api/admin/performances/{performanceId}/home-tags
```
**응답 예시**:
```json
[
  {
    "tag": "BEST_MUSICAL",
    "section": "BEST_PERFORMANCE",
    "sectionDisplayName": "베스트 공연",
    "displayName": "베스트 뮤지컬"
  },
  {
    "tag": "NEW_THIS_WEEK",
    "section": "NEW_PERFORMANCE",
    "sectionDisplayName": "신규 공연",
    "displayName": "이번주 신규"
  }
]
```

### 2. 홈 섹션 메타데이터 조회
```
GET /api/admin/home/sections/metadata
```
**응답 예시**:
```json
[
  {
    "section": "BEST_PERFORMANCE",
    "displayName": "베스트 공연",
    "tags": [
      { "tag": "BEST_MUSICAL", "displayName": "베스트 뮤지컬" },
      { "tag": "BEST_CONCERT", "displayName": "베스트 콘서트" }
    ]
  },
  {
    "section": "NEW_PERFORMANCE",
    "displayName": "신규 공연",
    "tags": [
      { "tag": "NEW_THIS_WEEK", "displayName": "이번주 신규" },
      { "tag": "NEW_THIS_MONTH", "displayName": "이번달 신규" }
    ]
  }
]
```

### 3. 홈 태그 추가/수정
```
POST /api/admin/performances/{performanceId}/home-tags
```
**요청 바디**:
```json
{
  "tag": "BEST_MUSICAL"
}
```

### 4. 홈 태그 삭제
```
DELETE /api/admin/performances/{performanceId}/home-tags/{tag}
```

## 🏗️ 기술 스택 & 아키텍처 (FSD)

### Layer 구조

#### Entities
- **entities/performance**:
  - `api/performance-home-tag.api.ts`: 홈 태그 CRUD API 함수
  - `api/performance-home-tag.queries.ts`: React Query hooks
  - `model/performance-home-tag.types.ts`: orval 생성 타입
  - `index.ts`: Public API export

- **entities/home-section** (신규):
  - `api/home-section.api.ts`: 섹션 메타데이터 API
  - `api/home-section.queries.ts`: React Query hooks
  - `model/home-section.types.ts`: orval 생성 타입
  - `index.ts`: Public API export

#### Features
- **features/admin/performance-home-tag-management** (신규):
  - `ui/PerformanceHomeTagManagement.tsx`: 태그 관리 모달 메인 컴포넌트
  - `ui/HomeTagSectionMultiSelect.tsx`: 섹션별 다중 태그 선택 컴포넌트 (Multi-select)
  - `ui/multi-select.tsx`: shadcn-multi-select-component (shared/ui에 설치)
  - `lib/useHomeTagForm.ts`: 폼 상태 관리 및 변경사항 추적 hook
  - `lib/useHomeTagMutations.ts`: 태그 추가/삭제 일괄 처리 hook
  - `index.ts`: Public API export

#### Views
- **views/admin/performance-management**:
  - `PerformanceDetailView.tsx`: 태그 표시 및 모달 버튼 추가

### 주요 컴포넌트

#### 1. PerformanceHomeTagManagement (Modal)
```typescript
interface PerformanceHomeTagManagementProps {
  performanceId: number;
  performanceTitle: string;
  isOpen: boolean;
  onClose: () => void;
  onSuccess?: () => void;
}
```

#### 2. HomeTagSectionMultiSelect
```typescript
interface HomeTagSectionMultiSelectProps {
  section: HomeSectionMetadata;
  selectedTags: string[]; // 선택된 태그들의 배열
  onChange: (tags: string[]) => void;
  disabled?: boolean; // 저장 중일 때 비활성화
}
```

#### 3. useHomeTagMutations Hook
```typescript
interface UseHomeTagMutationsReturn {
  saveChanges: (changes: TagChanges) => Promise<void>;
  isLoading: boolean;
  error: Error | null;
}

interface TagChanges {
  added: string[]; // 추가할 태그들
  removed: string[]; // 삭제할 태그들
}
```

## ✅ 개발 체크리스트

### 1. PRD & 문서
- [x] PRD 작성 완료

### 2. API & Entities
- [x] orval 타입 확인 및 필요시 생성
- [x] `entities/performance`에 홈 태그 API 추가
- [x] `entities/home-section` 생성 및 메타데이터 API 추가
- [x] React Query hooks 작성

### 3. Shared UI
- [x] shadcn-multi-select-component 설치
  ```bash
  # multi-select 컴포넌트를 shared/ui에 추가
  # https://github.com/sersavan/shadcn-multi-select-component
  ```

### 4. Features
- [x] `features/admin/performance-home-tag-management` 생성
- [x] 태그 관리 모달 컴포넌트 작성
- [x] 섹션별 Multi-Select 컴포넌트 작성
- [x] 폼 상태 관리 hook 작성 (변경사항 추적)
- [x] 태그 일괄 추가/삭제 mutation hook 작성
- [x] 저장 중 UI 상태 처리 (버튼 비활성화, 로딩)

### 5. Views
- [x] `PerformanceDetail`에 태그 표시 추가 (`sectionDisplayName-tagDisplayName` 형식)
- [x] 태그 관리 버튼 추가
- [x] 모달 연동

### 6. 테스트 & 검증
- [x] 태그 조회 기능 테스트
- [x] 다중 태그 선택/해제 기능 테스트
- [x] 태그 일괄 추가 기능 테스트
- [x] 태그 일괄 삭제 기능 테스트
- [x] 저장 중 UI 비활성화 동작 확인
- [ ] 부분 실패 시 에러 처리 확인
- [x] UI/UX 검증

## 📝 참고사항

### 아키텍처 & 코드 품질
- FSD 아키텍처 원칙 준수
- Public API를 통한 export만 허용
- 모든 함수/인터페이스에 JSDoc 주석 작성

### UI 컴포넌트
- Shadcn UI 컴포넌트 활용 (Dialog, Badge, Button)
- [shadcn-multi-select-component](https://github.com/sersavan/shadcn-multi-select-component) 사용
- 태그 표시 형식: `sectionDisplayName-tagDisplayName`

### 데이터 처리
- React Query를 통한 서버 상태 관리
- 여러 API 호출 시 일괄 처리 (Promise.all 또는 순차 처리)
- 저장 중 UI 잠금 처리 필수:
  - 저장/취소 버튼 비활성화
  - 모달 닫기(X) 버튼 비활성화
  - Multi-select 컴포넌트 비활성화
- 부분 실패 시 성공/실패 구분하여 사용자에게 피드백
